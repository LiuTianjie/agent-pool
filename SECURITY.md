# Agent Pool 安全与隐私边界

Agent Pool 的默认 Runner 模式面向“愿意贡献自己本地 Agent 算力的普通用户”。它尽量减少任务内容暴露，但不把普通用户自有电脑描述成可信执行环境。

## 当前版本提供的保护

- 平台只签发 Agent Pool Runner 令牌；Codex、Claude 等本地登录凭据不会上传到平台。
- 任务说明、Unit 输入、期望输出和交付结果在数据库中使用 AES-256-GCM 加密保存。
- Runner 一次只取得已租用的一个 Unit，不会下载整个任务池。
- 所有任务领取都要求主人创建有界 Claim；Claim 同时绑定 Runner 凭证、具体节点、Pool、数量上限和过期时间。注册、心跳和能力认证本身不会派单。
- CLI 在领取前展示单 Unit 时限、重试次数、验收方式、输出上限和 Webhook 域名，并在创建 Claim 前用固定字段再次确认；发布者自述的标题/摘要会移除终端控制与双向文本控制字符。
- CLI 默认不打印任务输入或模型输出，不把它们写入 Runner 日志。
- 每个 Unit 使用新的临时目录和不持久化的 Agent 会话；结束后立即清理目录。
- Codex 使用只读沙箱并显式关闭 shell/unified-exec；Claude 显式关闭 tools、MCP、设置源和斜杠命令。
- 发布方要求的 Adapter 与模型必须精确匹配；不做静默降级或模型替换。
- 能力认证只记录指定 Adapter/模型的正确率、并发和延迟，不收集模型密钥。
- `agentpool logout` 会撤销服务端 Runner 凭证并下线关联节点，而不只是删除本地文件。
- Task Capsule 的合同 hash 会绑定 Lease 与外部回执；Runner 会在执行前复算并核对该 hash，也不负责填写或选择 Unit ID。
- 直达 Webhook 只接受公开 HTTPS 地址，Runner 会解析并固定目标 IP、拒绝本地/私网/链路本地地址，并且不跟随重定向。
- Official Fleet 的中转 Route 密钥只能通过宿主环境变量或绝对 secret file 引用；每个子进程只获得当前 Route 的最小环境，不继承其他 Route 或平台进程的密钥。
- Official Fleet device approval 必须先由登录用户预览后端派生的 Official 身份，并二次确认；批准事务会核对预览时的 client/operator type，邮箱本身不能提权。

## 当前版本不承诺的保护

普通自有机器不是机密计算环境。拥有该机器的 root、调试、内核或进程检查权限的人，仍可能观察进程参数、标准输入、内存、网络或本地 Agent 本身。因此：

- “密封线程”表示产品界面、Runner 日志和会话生命周期上的隔离，不表示对恶意宿主的密码学保密。
- Benchmark 能证明该节点在测试时提交了正确结果和可观测性能，不能密码学证明底层一定是它声称的模型。
- 当前版本不适合把无法向第三方计算节点披露的密钥、身份证件、医疗信息或其他高敏感明文直接放入任务。
- 发布者编写的任务说明属于不可信输入；即使关闭了任务工具，也不能把普通宿主机视为安全隔离区。
- Agent 子进程仍运行在 Runner 账号的主机环境中；专用低权限账号是当前隔离模型的一部分，而不是可选的加固装饰。
- 启用 `--allow-webhooks` 会让发布者控制的 callback 看到 Runner 的出口 IP。默认关闭该能力只能避免无意暴露，不能在启用后隐藏网络来源。
- Webhook 发布者在收到产物后仍可能不返回回执或恶意拒绝；签名回执能防止 Runner 伪造“已验收”，但不能替代发布者信誉、争议仲裁或托管式质量裁决。

需要强保密的任务应等待托管可信 Runner：在受控的机密虚拟机或 TEE 中运行，并结合远程证明、短期密钥和受约束的网络出口。该模式不属于当前 MVP。

## 发布者建议

- 尽量把数据去标识化，只提供完成单个 Unit 所需的最小上下文。
- 不要在任务说明中放 API Key、Cookie、私钥或长期访问令牌。
- 使用 JSON Schema 或确定性期望结果自动验收；对高价值结果进行抽样复核。
- Webhook callback 必须按 `leaseId` 幂等处理重复投送、妥善保护回执密钥，并且不要把任务内容写进可公开访问的错误日志。
- 把任务拆成可单独重试、单独结算、泄露半径有限的小 Unit。

## Runner 主人建议

- 用一个没有个人文档、SSH Key、浏览器资料和其他项目凭据的专用低权限系统账号运行 Runner。
- 不要以 root 身份运行 Runner；不要给该账号挂载与接单无关的目录。
- 把任务内容视为第三方不可信输入；发现异常任务时立即下线节点并向平台报告。
- 先用 `agentpool jobs` 查看公开摘要，只对明确选择的 Pool 和数量执行 `agentpool claim`；不要把旧式无限 `online` 包装成守护进程。

## 分发边界

安装器会校验同源 SHA-256，能发现传输损坏，但同源校验和不是发布者签名。正式大规模推广 Runner 前，需要增加签名产物、固定版本和可复核的发布链。
