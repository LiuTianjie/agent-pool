PORT=3000
DATABASE_URL=postgresql://agentpool:agentpool@localhost:5432/agentpool
JWT_SECRET=replace-with-at-least-32-random-characters
# openssl rand -base64 32
TASK_ENCRYPTION_KEY=replace-with-32-byte-base64-or-64-hex-key
APP_ORIGIN=http://localhost:3000
ALLOW_DEV_TOPUP=true
# Optional: path to the built web app. If present, the API serves it as a SPA.
WEB_DIST_PATH=
